eureka:
  client:
    service-url: 
       defaultZone: http://localhost:8000/eureka/
  instance:
    hostname: localhost
       
spring:
  cloud:
    config:
      label: dev
      profile: dev
      discovery.serviceId: config-server
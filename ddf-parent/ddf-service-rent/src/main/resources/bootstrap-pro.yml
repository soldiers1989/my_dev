eureka:
  client:
    service-url: 
       defaultZone: http://localhost:8000/eureka/
  instance:
    hostname: ddf-service-rent
       
spring:
  cloud:
    config:
      label: master
      profile: pro
      discovery.serviceId: config-server